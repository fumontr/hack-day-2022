# ui (frontend)

### start dev server

`$ npm install`
`$ npm start`

`http://localhost:5173/play` が開発中

### デプロイ

（`$ sudo npm i -g firebase-tools` `$ firebase login`）

`$ cd ui`
`$ firebase deploy`
